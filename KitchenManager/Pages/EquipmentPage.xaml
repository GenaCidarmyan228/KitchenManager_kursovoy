<Page x:Class="KitchenManager.Pages.EquipmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
    
      Title="Склад оборудования" Loaded="Page_Loaded" Background="{StaticResource MainBackground}" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Добавить товар" Width="150" Height="30" Margin="0,0,10,0" Click="BtnAdd_Click" Style="{StaticResource GreenBtnStyle}"/>

            <Button Content="Изменить" Width="100" Height="30" Click="BtnEdit_Click" Background="#FFA726"/>

            <Button Content="Удалить" Width="100" Height="30" Click="BtnDelete_Click" Style="{StaticResource RedBtnStyle}"/>
        </StackPanel>

        <DataGrid x:Name="DGridEquipment" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="10" BorderBrush="Gray" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat='{}{0:N2} ₽'}" Width="100"/>

                <DataGridTextColumn Header="На складе" Binding="{Binding StockQuantity}" Width="100" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>